import React from "react";
import { withRouter, RouteComponentProps } from "react-router";

interface PalantirProps extends RouteComponentProps {}

const Palantir: React.FC = ({ location }) => {
    return <div></div>;
};

export default withRouter(Palantir);
